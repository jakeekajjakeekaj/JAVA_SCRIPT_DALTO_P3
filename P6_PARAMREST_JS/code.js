// PARAM REST, esta es la manera de usar un PARÁMETRO CON ARGUMENTS
//DESTRUCTURACIÓN

// "strict mode";

// const suma = (frase, ...num)=> {   //Gracias a esta sentencia, se pueden colocar los argumentos que se quieram, ya sea 1, 2, 3, 8, etc.; ASÍMISMO COMO YA SE EXPLICÓ, LOS ARGS SIEMPRE TIENEN QUE IR AL FINAL, (ES DECIR (...num, frase), ESTÁ MAL, YA QUE LOS ARGS SIEMPRE EBEN IR AL FINAL)
    // let resultado = 0;
    // for (let i = 0; i < num.length; i++) {  //El for es indispensable, ya que como no se sabe la cantidad de argumentos que se tiene, ya que elegimos que sea por argumentos, se tiene que hacer un corrimiento por todo el arreglo para así poder trabajar con los datos y por ejemplo realizar la suma
        // resultado += num[i];
    // }
    // console.log(`${frase} ${resultado}`);
// }

// suma("suman", 12, 23, 24);   //Aquí se indican los valores de entrada que se quieran, como se muestra, LOS ARGS SIEMPRE TIENEN QUE IR AL FINAL, SI SE TIENEN OTRO TIPO DE DATOS, SE TIENEN QUE INDICAR ANTES QUE LOS ARGS

// -- DESTRUCTURACIÓN

// INVESTIGAR POR TU CUENTA, SECCIÓN NO ENCONTRADA EN EL CURSO

// En la próxima sección se ve la manera de destructurar un array sin necesidad de usar la destructuración